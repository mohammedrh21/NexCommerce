@page "/categories/{Id:guid}"
@using Client.Components.Products
@using Client.Components.UI

<PageTitle>@(_translation?.Name ?? "Category") | NexCommerce</PageTitle>

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    @if (_isLoading)
    {
        <div class="animate-pulse space-y-8">
            <div class="h-10 bg-zinc-200 dark:bg-zinc-800 rounded w-1/4"></div>
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
                @for (var i = 0; i < 8; i++)
                {
                    <div class="h-80 bg-zinc-200 dark:bg-zinc-800 rounded-xl"></div>
                }
            </div>
        </div>
    }
    else if (!string.IsNullOrEmpty(_errorMessage))
    {
        <ErrorState Message="@_errorMessage" OnRetry="LoadCategoryAndProductsAsync" />
    }
    else if (_category != null && _translation != null)
    {
        <div class="mb-8">
            <nav class="flex mb-4 text-sm text-zinc-500 dark:text-zinc-400">
                <ol class="flex items-center space-x-2">
                    <li>
                        <a href="/" class="hover:text-indigo-600 dark:hover:text-indigo-400 transition-colors">Home</a>
                    </li>
                    <li>
                        <svg class="h-4 w-4 text-zinc-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                        </svg>
                    </li>
                    @if (_category.ParentId.HasValue && !string.IsNullOrEmpty(_category.ParentName))
                    {
                        <li>
                            <a href="/categories/@_category.ParentId" class="hover:text-indigo-600 dark:hover:text-indigo-400 transition-colors">@_category.ParentName</a>
                        </li>
                        <li>
                            <svg class="h-4 w-4 text-zinc-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                            </svg>
                        </li>
                    }
                    <li class="text-zinc-900 dark:text-white font-medium">@_translation.Name</li>
                </ol>
            </nav>

            <h1 class="text-3xl font-bold tracking-tight text-zinc-900 dark:text-white">
                @_translation.Name
            </h1>
            <p class="mt-2 text-sm text-zinc-500 dark:text-zinc-400">
                @_category.ProductCount products found in this category.
            </p>
        </div>

        @if (_isLoadingProducts)
        {
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 animate-pulse">
                @for (var i = 0; i < 8; i++)
                {
                    <div class="h-80 bg-zinc-200 dark:bg-zinc-800 rounded-xl"></div>
                }
            </div>
        }
        else if (_productsResult?.Data == null || !_productsResult.Data.Any())
        {
            <EmptyState Title="No products found" 
                        Description="There are currently no products in this category." 
                        Icon="M20.25 7.5l-.625 10.632a2.25 2.25 0 01-2.247 2.118H6.622a2.25 2.25 0 01-2.247-2.118L3.75 7.5m8.25 3v6.75m0 0l-3-3m3 3l3-3M3.375 7.5h17.25c.621 0 1.125-.504 1.125-1.125v-1.5c0-.621-.504-1.125-1.125-1.125H3.375c-.621 0-1.125.504-1.125 1.125v1.5c0 .621.504 1.125 1.125 1.125z" />
        }
        else
        {
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 mb-8">
                @foreach (var product in _productsResult.Data)
                {
                    <ProductCard Product="@product" />
                }
            </div>

            @if (_productsResult.TotalPages > 1)
            {
                <div class="flex justify-center mt-12">
                    <AppPagination CurrentPage="@_productsResult.Page" 
                                   TotalPages="@_productsResult.TotalPages" 
                                   OnPageChanged="OnPageChanged" />
                </div>
            }
        }
    }
</div>
